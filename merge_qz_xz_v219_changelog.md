# v2.19 西藏自治区 / 青海省 批次合并修改日志

- 数据源：`各省市细分站点/西藏自治区、青海省/青海省、西藏自治区客运站点总表.xlsx`（单 sheet 名为 `Sheet`，标题行非首行须扫描定位「车站名称」标题行；仅含 拉林铁路 中段 5 站）
- 基线：`src/railway_data_v2.18.bak`（v2.18，628 线 / 2635 站）
- 合并脚本：`merge_qz_xz_v219.py`（增量 only-add；可重复回放）

## 规模变化

| 指标 | v2.18 | v2.19 | 增量 |
|---|---|---|---|
| LINE_ORDER 线路 | 628 | **628** | +0（仅重建既有 拉林铁路 线序，无新线） |
| STATION_INFO 车站 | 2635 | **2640** | +5（拉林铁路中段 5 新客运站） |
| CITY_TO_STATIONS 城市键 | 743 | **745** | +2（山南市 / 林芝市） |
| CITY_ALIAS 别名 | 1874 | **1895** | +21（西藏地级市键及对应裸名/省前缀别名） |
| GRAPH 节点 | 2631 | **2640** | +9（5 新站 + 既有 拉林铁路 线序重连导致的边变化） |

## 数据源特殊处理

1. **标题行不在第 0 行**：扫描定位「车站名称」标题行，从其下一行取数据（与甘宁批同款坑）。
2. **站名无「站」后缀**：`norm_sta` 统一补后缀；括号别名取正名。
3. **城市键格式（西藏）**：裸地级市键（`山南市` / `林芝市`），省份键 `西藏`；`parse_city` 对 `西藏自治区` / `西藏` 前缀分支处理。
4. 备注含「不办理旅客乘降」的行排除（本批无此类行）。

## 既有线精确重建（拉林铁路，0 新线、0 SYN）

现有 v2.18 网络**已含完整拉林铁路链**（拉萨→协荣→山南→朗县→米林→林芝）。本批 5 站按 Excel 同线顺序与 prev/next **精确插入既有线序列**，无需 SYN 接网：

| 线路 | 重建后完整序列 |
|---|---|
| 拉林铁路 | 拉萨站 → 协荣站 → **贡嘎站** → **扎囊站** → 山南站 → **桑日站** → **加查站** → 朗县站 → 米林站 → **岗嘎站** → 林芝站 |

（加粗为本次新增的 5 站：贡嘎/扎囊/桑日/加查/岗嘎）

## 验证结果

- 引擎自测 **27/27 PASS**，连跑 3 次无抖动。
- 5 新站全部可达拉萨 / 林芝（并入既有连通链，无孤岛）。
- LINE_ORDER↔lines 归属一致性：**本批新增 MISS = 0**（库内既有 361 条 `__SYN__` 历史遗留不变）。
- 未解析枢纽 8 个，与 v2.18 持平（无回归）。
- 7 个 `data/*.json` 镜像已同步。

---

## 更正记录（2026-07-30 补）

> 复盘时发现本批次合并脚本 `merge_qz_xz_v219.py` 的 `parse_city` 存在**前缀切片 bug**：
> 对 `西藏自治区` 前缀使用 `s[6:]` 截取，但「西藏自治区」实为 **5 个字符**，导致城市名首字被吞。
> 直接后果：拉林 5 站的 `city` 键被误截为 `南市`（应为 `山南市`）和 `芝市米林市`（应为 `林芝市`），
> 进而这 5 站被挂到错误城市键下，`CITY_TO_STATIONS` 中正确的 `山南市`/`林芝市` 组缺失这 5 站。
> 用户以「山南市」/「林芝市」为优惠区间端点时，无法匹配到这 5 站（真实正确性缺陷）。

**已修正**（写于 v2.20 数据层，见 `fix_v219_citykeys.py`）：

- 5 站 `city` 键归正：`贡嘎站/扎囊站/桑日站/加查站 → 山南市`，`岗嘎站 → 林芝市`。
- 清理错误城市键 `南市` / `芝市米林市` 及其在 `CITY_TO_STATIONS` / `CITY_ALIAS` 中的映射；确保 `山南`→`山南市`、`林芝`→`林芝市` 别名在位。
- 仅更正城市键归属，**未增删任何站 / 线 / 边**；版本号仍为 v2.20。
- 修正后复跑自测 **27/27 ×3 无抖动**；`resolve_location('山南市')` 现返回 5 站、`resolve_location('林芝市')` 含岗嘎站。
- 合并脚本 `merge_qz_xz_v219.py` 的 `parse_city` 前缀切片已改为按实际长度（`西藏自治区`→`s[5:]`、`西藏`→`s[2:]`），仅供文档追溯，勿在已含 v2.20 的数据上重跑该脚本。
