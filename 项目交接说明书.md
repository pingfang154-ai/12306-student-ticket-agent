# 12306 学生票合规判定 Agent — 项目交接说明书（上下文压缩）

> 用途：供新对话中的智能体快速理解项目现状、架构与接手方式。本文档是"精炼版"，细节以代码、`*_changelog.md`、`.workbuddy/memory/MEMORY.md` 为准。
> 工作根目录：`C:\Users\cjp15\Desktop\全国客运站点\交接文件夹第三版（含一、二合并）\`
> 数据层当前版本：**v2.11**（499 线 / 2002 站），27/27 回归通过。

---

## 1. 项目概述与目标

**是什么**：判断"学生购买的火车票区间是否在其学生证优惠区间（学校城市 ↔ 家庭城市）内"的合规引擎，附带 Web 演示界面。

**核心判定逻辑**：
- 优惠区间 = 学校城市 ↔ 家庭城市（学校所在地不可改，家庭可改）。
- 购买区间合规 ⇔ 出发站、到达站都能映射到优惠城市集合，且存在一条铁路径路顺序包含二者（同向或反向均可）。
- 支持：同城多站兼容（"北京"→北京站/北京西/北京南…）、反向购票、联程中转（5 自然日内不限换乘次数）、显式途经站 `via_stations` 廊道探测。
- 不合规时给出修改建议（改终点站 / 反向购票 / 改家庭地）。

**当前状态**：自包含可运行。已覆盖全国主要干线及多批省份补充站点（贵州/湖北/四川重庆/云南/山东青海/其余 8 市/武宜段/广西/湖南+广东+香港/江西+福建）。

---

## 2. 已完成的关键改动列表（及目的）

### 2.1 基础架构（第三版 = 第一版 + 第二版合并）
| 改动 | 目的 |
|---|---|
| 合并一/二版为自包含工程 | 统一代码与数据，消除分散 |
| 补回 `student_rules.json` | 修复引擎 import 失败（路径优先 `src/` 再 `data/`） |
| 修复 Starlette `TemplateResponse` 新签名 | 消除 Web HTTP 500（`TemplateResponse(request, name, context)`） |
| 建立 27 条回归测试 `src/test_cases.py` | 每次数据改动的守门标准：`python -u src/student_ticket_checker.py --test` |

### 2.2 数据层省份增量合并（铁律：只加不删，新线必须写 `LINE_ORDER`）
| 里程碑 | 内容 | 解决的核心问题 |
|---|---|---|
| 贵州 / 湖北 / 四川重庆 / 云南 / 山东青海 / 其余 8 市 / 武宜段 | 各批站点与线路 | 补全西南、华中、北方、主干补线；川渝批 Y 型分支补 107 条合成线修复线性化丢边 |
| **武汉→成都经重庆修复 (v2.8)** | 补「宁蓉铁路」走廊，统一「重庆」城市键 | 原引擎把武汉→成都绕开重庆，修复为经重庆的合理径路 |
| **广西合并 (v2.9)** | 17 站 / 7 线 | 城市键用「广西X市」前缀（与其它批不同），首次大规模验证合并 SOP |
| **湖南+广东+香港 (v2.10)** | 219 站 / 52 线 | 长株潭城际「人字形」分支拆线 + 29 条合成线接网；广州白云站解析（枢纽 13→12，净改善） |
| **江西+福建 (v2.11)** | 50 站 / 23 线 | 16 全新线 + 6 既有线精确插入 + 16 条合成线；修复 `parse_city` 贪婪正则 bug |

### 2.3 通用能力
- **双格式城市别名**（`augment_aliases_v210.py`）：为所有城市键补 `裸名 ↔ 省份+裸名`（如 `长沙市` ↔ `湖南长沙市`），提升 Web 容错；清理 3 字省份加倍前缀别名（如 `内蒙古内蒙古…`）。
- **连通性修复 Pass**：孤立新站按 tier1 已可达锚点 → tier2 同线旧锚 → tier3 同城旧站 → tier4 枢纽兜底补 `__SYN__` 合成线。**关键：tier1 仅接受"已可达"锚点，避免两个孤立站互连成孤立对。**

### 2.4 Web 界面
- **FastAPI + Starlette + Jinja2**（`src/web_app.py`），uvicorn 启动。
- 路由：`/` 主页、`/cases` 案例库、`/check?school=&home=&dep=&arr=&waypoints=` 直接渲染（支持 GET 带参、途经站预填充）。
- UI 规范：铁路蓝主色 `hsl(205,72%,52%)` #2F8FD9；马卡龙辅助色；sticky 导航 + Hero + 卡片结构。改 UI 优先动 `web/static/style.css` 与 `templates/*.html`，保持 `web_app.py` 不动。

### 2.5 GitHub 同步
- 本地 git 仓库，远程私有库 `pingfang154-ai/12306-student-ticket-agent`。
- 已推送分支 **`v2.10`**（commit 8e12ccf）与 **`v2.11`**（commit 0de0381），均未改 `main`。
- **关键约束**：沙箱内 `git push` 被丢弃，须用 **GitHub Git Data API** 推送（blobs→trees→commits→refs；分支已存在用 PATCH + `force:true`）。详见 MEMORY.md。

---

## 3. 当前项目架构与技术栈

**目录（根 = 第三版文件夹）**
- `src/railway_data.py` — **核心数据层**：7 个单行 dict（META / LINE_ORDER / STATION_INFO / CITY_TO_STATIONS / PROVINCE_TO_STATIONS / CITY_ALIAS / GRAPH）+ helper（`resolve_location` / `_norm` / `LINE_NAME_ALIAS`）。当前 v2.11。
- `src/student_ticket_checker.py` — **合规引擎**（~904 行）：Dijkstra `_dijkstra`、Yen's KSP `find_multiple_paths_between_cities`、廊道探测（`check_compliance` 内 `via_stations` 分支）。
- `src/web_app.py` — Web 服务（FastAPI）。
- `src/test_cases.py` — 27 条回归测试。
- `data/` — JSON 同步（city_to_stations / graph_adjacency / lines_order / station_info / city_aliases / station_aliases / hub_stations / railway_data / student_rules）。
- `web/` `templates/` `static/` — 前端资源。
- 合并脚本 `merge_*.py`、诊断 `diag_*.py`、验证 `validate_*.py`、`*_changelog.md`。

**技术栈**
- Python 3.13（managed venv：`C:\Users\cjp15\.workbuddy\binaries\python\envs\default`，运行前设 `PYTHONIOENCODING=utf-8` 防 GBK 乱码）。
- FastAPI / Starlette / Jinja2；openpyxl（读 Excel）；邻接表自实现（非强制 networkx）。

**常用命令**
```bash
# 回归测试（每次数据改动后必跑，须 27/27）
python -u src/student_ticket_checker.py --test

# 启动 Web（当前 v2.11 端口）
uvicorn src.web_app:app --port 8085 --host 127.0.0.1
```

**端口映射**（旧进程占用，沙箱无法 `taskkill`）：v2.6=8080 / v2.7=8081 / v2.8=8082 / v2.9=8083 / v2.10=8084 / **v2.11=8085**。宿主机释放后可回 8080。

**数据层铁律（改动时必守）**
1. Dijkstra 只走"有线路的边" → 合成/干线走廊线必须写入 `LINE_ORDER`，不可往 `GRAPH` 直接加裸边。
2. 增量合并，只加不删（`to_add = merged_edges - original_edges`）。
3. 再生 `railway_data.py` 按内容标记截取 tail（以 `LINE_NAME_ALIAS` / `def resolve_location` 为起点），不用行号。
4. 每次数据改动后必跑 27 条回归。
5. 枢纽解析数不得减少（固有 12 个未解析站不视为回归）。

**版本历史**
| 版本 | 线路 | 车站 | 关键合并 | 未解析枢纽 |
|---|---|---|---|---|
| v2.8 | — | — | 武汉→成都经重庆 | 13 |
| v2.9 | 397 | 1738 | 广西 17 站/7 线 | 13 |
| v2.10 | 467 | 1952 | 湖南+广东 219 站/52 线 | 12（净改善） |
| **v2.11** | **499** | **2002** | **江西+福建 50 站/23 线** | **12（与 v2.10 一致）** |

---

## 4. 尚未完成 / 已知问题

- **未覆盖省份**：全国大量省份批次尚未合并（河南/河北/陕西/山西/江苏/浙江/安徽/东北三省/新疆/西藏/甘肃/宁夏/内蒙古等），待用户分批给 Excel 续做。
- **线路顺序非真实时刻序**：部分线路依赖手动整理/合成线，非真实运行方向序；每年需复核新线。
- **固有 12 个未解析枢纽**：重庆东/遵义南/石家庄北/哈尔滨东/齐齐哈尔南/武汉东/徐州北/扬州东/呼和浩特/大同东/重庆沙坪坝/北京东（既有清单，查这些站 Web 报"无法识别"，非回归）。
- **沙箱网络限制**：`git push` 不可用，须走 Git Data API；宿主代理 `127.0.0.1:10808` 沙箱不可达。
- **端口累积占用**：每次升版起新端口，旧端口未回收（宿主机 `taskkill` 可解）。
- **Web 仅本地**：无公网访问（如需对外可走 CloudStudio / htmlcode.fun 部署）。

---

## 5. 下一步计划 / 待办

1. **继续省份合并**：用户给出下一批 Excel（如"河南省…"），沿用技能 `railway-data-province-merge` SOP 增量合并，升 v2.12+。
2. **年度复核**：新开通线路/车站（京沪/京广/徐兰/沪昆/兰新/沿海通道优先）。
3. **（可选）收口分支**：GitHub 上将 `v2.11` 合并进 `main`（开 PR 或 force）。
4. **（可选）修复固有未解析枢纽**：补别名/线路使 12 站可达（非必须）。
5. **（可选）Web 公网部署**：CloudStudio 或静态发布便于演示。

---

## 附：关键文件速查
- 数据层：`src/railway_data.py`（v2.11）
- 引擎：`src/student_ticket_checker.py`
- 测试：`src/test_cases.py`（`--test`，27 条）
- 最新变更日志：`merge_jx_fj_v211_changelog.md`（v2.11）
- 合并 SOP 技能：`~/.workbuddy/skills/railway-data-province-merge/SKILL.md`
- 项目记忆（细节/坑/铁律）：`C:\Users\cjp15\Desktop\全国客运站点\.workbuddy\memory\MEMORY.md`
- 每日记录：`C:\Users\cjp15\Desktop\全国客运站点\.workbuddy\memory\2026-07-26.md`

## 新智能体接手 30 秒速查
1. 先读 `MEMORY.md`（铁律/坑/省份合并策略）与本文档。
2. 改数据前 `cp src/railway_data.py src/railway_data_v2.11.bak` 备份。
3. 仿 `merge_jx_fj_v211.py` 写新合并脚本（读 Excel → 写 LINE_ORDER/STATION_INFO/CITY_TO_STATIONS/PROVINCE_TO_STATIONS/CITY_ALIAS → 重建 GRAPH → 连通性修复）。
4. 跑 `python -u src/student_ticket_checker.py --test` 须 27/27；新站 `resolve_location` 非空；新城市连通北上广；枢纽解析不劣于上版。
5. 起 Web 用新端口；交付须附可查验链接；推 GitHub 用 Git Data API（非 `git push`）。
