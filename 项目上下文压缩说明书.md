# 12306 学生票合规判定 Agent — 项目上下文压缩说明书

> **用途**：供新对话智能体快速接手本项目。浓缩自 v2.6→v2.20 的全部增量合并工作，重点覆盖本对话（任务七~九：甘肃/宁夏、西藏/青海、新疆）。
> **生成日期**：2026-07-29
> **项目铁律复盘**：本项目有一组不可违背的工程铁律（见附录 A），新智能体在任何改动前必须先读附录 A。

---

## 0. 30 秒速查（新智能体先看这里）

| 项 | 值 |
|---|---|
| 项目根（实际工作目录） | `C:\Users\cjp15\Desktop\全国客运站点\交接文件夹第三版（含一、二合并）\` |
| 数据层单文件 | `src/railway_data.py`（当前 **v2.20**） |
| v2.20 规模 | **641 线 / 2657 站 / 753 城市键 / 1911 别名 / 2657 GRAPH 节点 / 33 省份键** |
| 引擎自测命令 | `cd src && python -u student_ticket_checker.py --test` → 必须 **27/27 PASS** |
| Web 查验启动 | `cd src && uvicorn web_app:app --host 0.0.0.0 --port 8091`（v2.20 用新端口） |
| 合并 SOP 技能 | `railway-data-province-merge`（用户级 `~/.workbuddy/skills/`） |
| Python 运行时 | 托管 venv：`C:\Users\cjp15\.workbuddy\binaries\python\envs\default\Scripts\python.exe` |
| 数据源目录 | `C:\Users\cjp15\Desktop\全国客运站点\各省市细分站点\`（各省/自治区子文件夹） |
| 当前未解析枢纽 | **8 个**（历史固有清单，不视为回归） |
| lines 归属缺失 | 库内 **361 条历史遗留**（SYN 线导致）；断言应为「本批新增 MISS = 0」 |

---

## 1. 项目概述与目标

**这是什么**：一个 12306 学生票优惠区间合规判定引擎。输入「优惠区间（学校城市 A → 家庭城市 B）」与「期望购票区间（出发站 C → 到达站 D）」，引擎在**全国铁路图**上搜索路径，判定购票区间是否符合学生票优惠规则（含同城兼容、反向购票、联程中转、KSP 遮蔽兜底等）。

**核心数据层**：`src/railway_data.py` 是单文件 Python 字典库，包含全部车站、线路、邻接关系、城市映射。所有省份客运站点数据**分批增量合并**进该文件。

**推进方式**：用户按「任务 N」逐省下发 Excel（如「甘肃省、宁夏回族自治区客运站点总表.xlsx」），智能体按 SOP 增量合并、跑自测、写修改日志、启动 Web 并附 `/check` 查验链接。版本号随批次递增（v2.17→v2.18→v2.19→v2.20）。

**本对话已完成的批次**（任务七~九）：
- **任务七 / v2.18**：甘肃、宁夏（最大批量，107 行 / 104 客运站）
- **任务八 / v2.19**：西藏、青海（仅拉林铁路中段 5 站）
- **任务九 / v2.20**：新疆（20 客运站 / 9 线标签）

---

## 2. 已完成的关键改动列表及目的

### 2.1 主线版本历史（节选自项目长期记忆，v2.8 起）

| 版本 | 线 | 站 | 关键合并 | 未解析枢纽 |
|---|---|---|---|---|
| v2.8 | — | — | 武汉→成都经重庆（补「宁蓉铁路」走廊） | 13 |
| v2.9 | 397 | 1738 | 广西 17 站/7 线 | 13 |
| v2.10 | 467 | 1952 | 湖南+广东 219 站/52 线 | 12 |
| v2.11 | 499 | 2002 | 江西+福建 50 站/23 线 | 12 |
| v2.12 | 524 | 2096 | 沪苏皖浙 94 站 | 12 |
| v2.13 | 530 | 2152 | 河南 56 站 | 12 |
| v2.14 | 561 | 2220 | 山东 68 站 | 12 |
| v2.15 | 572 | 2294 | 陕西 74 站 | 11（扬州东已解析） |
| v2.16 | 585 | 2372 | 山西 78 站 | 11 |
| v2.17 | 618 | 2535 | 河北/北京/天津 163 站 | 11 |
| **v2.18** | **628** | **2635** | **甘肃/宁夏 104 站** | **8（净改善）** |
| **v2.19** | 628 | 2640 | **西藏/青海 5 站（拉林铁路中段）** | 8 |
| **v2.20** | **641** | **2657** | **新疆 20 站 / 9 线** | 8 |

### 2.2 本对话三批次详情（任务七~九）

#### 任务七 → v2.18（甘肃 / 宁夏）
- **目的**：补全西北路网最大空白，104 个客运站一次并入。
- **规模**：628 线（+10：7 真实新线 + 3 SYN）/ 2635 站（+100）/ 743 城市键（+48）/ 1874 别名（+816）。
- **7 条新真实线**：西平铁路、中川城际铁路、干武铁路、红会支线、嘉镜铁路、敦格铁路、酒额铁路。
- **3 条 SYN 合成线（接网用）**：`__SYN__西平铁路__泾川站__平凉南站`、`__SYN__敦煌铁路__柳沟站__瓜州站`、`__SYN__兰新铁路__疏勒河站__柳沟站`。
- **首跑修复的两处缺口**：
  1. **4 个 orphan 站**（在库但无边）：大坝/惠安堡/环县/甜水堡 → 补 SPLICE（银西高铁曲子后+环县/甜水堡/惠安堡；包兰铁路黄羊滩—青铜峡间插大坝）。
  2. **敦煌子网孤岛**（敦煌/柳沟/瓜州/阿克塞/肃北 内部互连但到不了主干）→ 补 `__SYN__兰新铁路__疏勒河站__柳沟站` 接回兰新主干。
- **数据源坑（已沉淀进技能）**：标题行非第 0 行；站名无「站」后缀；宁夏「所属省市」短前缀「宁夏」；甘肃裸市键（兰州市）vs 宁夏「宁夏+市」键（宁夏银川市）；线路名变体（包兰线/干武线/宝兰客专/柳敦段）。
- **验收**：27/27 连跑 3 次零抖动；104 新站 0 orphan、0 不可达、解析 0 空；本批新增 lines-MISS=0；未解析枢纽 8（净改善）。

#### 任务八 → v2.19（西藏 / 青海）
- **目的**：补全拉林铁路中段缺站（文件仅 5 站：贡嘎/扎囊/桑日/加查/岗嘎）。
- **规模**：2640 站（+5），线数不变（628）。
- **做法**：现有 v2.18 网络**已有完整拉林铁路链**（拉萨→协荣→山南→朗县→米林→林芝），本批 5 站按 Excel 同线顺序 + prev/next **精确插入既有线**，**无需 SYN 接网**。
- **城市键**：裸名（山南市/林芝市）；省份键「西藏自治区 / 青海省」。
- **验收**：27/27；5 站全部可达拉萨/林芝；lines 归属缺失仍 361（历史遗留，无新增）。

#### 任务九 → v2.20（新疆）
- **目的**：补全新疆路网 20 客运站（9 线标签）。
- **规模**：641 线（+13，其中「和若铁路」在更早版本已注册，故净 +13 而非 +14）/ 2657 站（+17 新站；20 文件站 − 3 复用站 双河市/魏家泉/准东）。
- **6 条新真实线**：和若铁路、南疆铁路（图木舒克支线）、阿阿铁路、博州支线铁路、奎北铁路、乌准铁路。
- **8 条 SYN 接网**：格库↔铁干里克—若羌、和若↔昆玉—和田、图木舒克支线↔图木舒克—巴楚、阿阿↔阿瓦提—阿克苏、博州支线↔博乐东—精河、奎北↔四棵树—奎屯、乌准↔魏家泉—乌鲁木齐、兰新↔玛纳斯—乌鲁木齐。
- **既有线精确插入**：兰新铁路+达坂城/乌西、南疆铁路+鱼儿沟/和静、格库铁路重建骨架（米兰/铁干里克等）。
- **bug 修复**：`昆玉站` 城市键被贪婪正则误吞——「新疆生产建设兵团第十四师昆玉市」中「第十四师」为多字，原 `^新疆生产建设兵团第.师` 仅匹配单字师号。改为 `[^师]*师` 后修复，城市键归位「新疆昆玉市」。
- **城市键格式**：新疆用「新疆+地级市/地区/州/兵团市」，含自治州→简写映射（昌吉回族自治州→新疆昌吉州）、兵团师市归一（第三师图木舒克市→新疆图木舒克市 等）。
- **验收**：27/27 连跑 3 次零抖动；20 站全部 inSI+可达+可解析；本批新增 lines-MISS=0；14 条新线全部注册（含已存在的和若铁路）。

---

## 3. 当前项目架构与技术栈

### 3.1 数据层 `src/railway_data.py`（单文件字典）
| 字典 | 结构 | 用途 |
|---|---|---|
| `META` | dict | 元信息 |
| `LINE_ORDER` | `line -> [station, ...]` | **有序线路序列，Dijkstra 只走这里注册的边** |
| `STATION_INFO` | `station -> {province, city, lines:[...], ...}` | 站元数据；`lines` 必须与该站所在 LINE_ORDER 一致 |
| `CITY_TO_STATIONS` | `city_key -> [stations]` | 同城车站组 |
| `PROVINCE_TO_STATIONS` | `province -> [stations]` | 省份车站组 |
| `CITY_ALIAS` | `alias -> canonical` | 裸名↔全键 别名 |
| `GRAPH` | `station -> [neighbor, ...]` | 邻接表（path search 用） |
| tail | `LINE_NAME_ALIAS`、`def resolve_location(q)` | 线路名别名归一；输入解析 |

**铁律**：任何新连接必须是真实线（含 2 站 `__SYN__` 线）；**裸 GRAPH 边不可见**，必须写入 `LINE_ORDER`。再生文件时按内容标记（`LINE_NAME_ALIAS` / `def resolve_location`）截取 tail，**绝不用行号**（曾因行号截取致重复 `GRAPH =` 覆盖）。

### 3.2 引擎 `src/student_ticket_checker.py`
- 合规判定核心（含同城兼容、反向购票、联程中转、阶段四 KSP 遮蔽兜底）。
- `--test` 跑 27 条内置用例，**每次数据层改动后必须全过 27/27**。

### 3.3 Web `src/web_app.py`（Starlette/FastAPI + uvicorn）
- 路由：`/`（首页）、`/check`（GET/POST，参数 `school/home/dep/arr/new_home`，支持途经站预填充）、`/cases`（案例库）。
- 模板 `templates/*.html` + 静态 `static/style.css`；改 UI 优先动这两个，保持 `web_app.py` 不动。
- **Starlette `TemplateResponse` 新签名**：必须用 `TemplateResponse(request, name, context)`，旧签名抛 `TypeError`。

### 3.4 JSON 镜像 `src/data/*.json`（7 个）
`line_order` / `lines_order` / `graph_adjacency` / `station_info` / `city_to_stations` / `city_aliases` / `province_to_stations`，与 `railway_data.py` 同步。

### 3.5 技术栈
- Python 3.13.12（托管 venv）；`openpyxl` 读 Excel；Starlette/FastAPI + uvicorn；无 networkx（自研邻接 + BFS/Dijkstra/Yen KSP）。
- 合并 SOP 技能：`railway-data-province-merge`（已含甘宁批经验：标题行扫描、站名补后缀、短前缀解析、orphan/孤岛连通核查）。

---

## 4. 尚未完成的功能或已知问题

### 4.1 本对话产生的待补文档（非数据缺陷）
- ❌ **v2.19 changelog 未写**：`merge_qz_xz_v219_changelog.md` 缺失。
- ❌ **v2.20 changelog 未写**：`merge_xinjiang_v220_changelog.md` 缺失。
- ❌ **`MEMORY.md` 未追加 v2.19/v2.20** 行与 Web 端口（当前仅到 v2.18=8089）。
- ❌ **`2026-07-29.md` 日志未追加 v2.19/v2.20 记录**（仅 v2.18 已记）。
- ❌ **Web 服务未以 v2.20 重启**：端口 8089 进程（PID 12596）启动于 v2.18 时期，载入的是 v2.18 快照（uvicorn 无 `--reload` 不热更）。需用 v2.20 数据重启（建议新端口 **8091**）并补 `/check` 链接（铁律要求）。

### 4.2 历史固有问题（非回归，不视为缺陷）
- **8 个未解析枢纽**：历史清单中的部分站（原 12 个，已随批次净改善至 8），属既有清单，不视为回归。
- **361 条 lines-MISS 历史遗留**：SYN 合成线导致 `STATION_INFO[站].lines` 未含该 SYN 线名，库内既有。**断言标准应为「本批新增 MISS = 0」而非全库 = 0**。

### 4.3 沙箱限制
- 沙箱无法终止宿主机进程：端口占用只能在宿主机 `taskkill`，沙箱 bash/PowerShell 无效。
- 沙箱向 GitHub 推送走 Git Data API（`api.github.com` 可达，`github.com` 智能 HTTP 被丢弃）；本任务未涉及推送。

---

## 5. 下一步计划 / 待办事项

1. **写 v2.19 changelog** → `merge_qz_xz_v219_changelog.md`（拉林铁路 5 站精确插入既有线，无需 SYN）。
2. **写 v2.20 changelog** → `merge_xinjiang_v220_changelog.md`（6 新线 + 8 SYN + 昆玉城市键修复）。
3. **更新 `MEMORY.md`**：追加 v2.19/v2.20 行 + Web 端口（v2.19=8090、v2.20=8091）。
4. **更新 `2026-07-29.md`** 日志：追加任务八/九全过程。
5. **重启 Web 服务**（v2.20，端口 8091）并附代表性 `/check` 链接（铁律）：
   - 新疆代表：`/check?school=乌鲁木齐市&home=库尔勒市&dep=乌鲁木齐站&arr=库尔勒站`
   - 青藏代表：`/check?school=拉萨市&home=林芝市&dep=拉萨站&arr=林芝站`
6. **继续剩余省份合并**：查看 `各省市细分站点/` 下未处理子文件夹，按用户「任务 N」逐批推进（沿用 `railway-data-province-merge` SOP）。
7. **年度例行检查**：新开通线路/车站，优先主干高铁（京沪/京广/徐兰/沪昆/兰新/沿海通道）。

---

## 附录 A：铁律与约定（新智能体必读）

1. **增量合并，只加不删**：`to_add = merged_edges - original_edges`，绝不删旧边/旧站。
2. **Dijkstra 只走有线路的边**：合成/干线走廊线必须写入 `LINE_ORDER`，不可直接往 `GRAPH` 加裸边。
3. **枢纽解析须与上一版一致**：可解析枢纽数不得减少；固有未解析站不视为回归。
4. **每次数据层改动后必跑** `python -u src/student_ticket_checker.py --test`（27 条）。
5. **再生 `railway_data.py` 按内容标记截取 tail**，用 `LINE_NAME_ALIAS`/`def resolve_location` 为起点，不用行号。
6. **✅ 每次任务结束必须附 Web 查验链接**（用户明确要求，覆盖「仅 Web 变更才附」旧规）：优先带参 `/check` 直达，其次 `/` 或 `/cases`；纯数据任务也要给一条代表案例的 `/check` 链接。
7. **城市键格式随省**：甘肃裸市键（兰州市）；宁夏/新疆用「省/区+市」（宁夏银川市 / 新疆乌鲁木齐市）；兵团师市归一（第三师→图木舒克市）；自治州→简写（昌吉回族自治州→新疆昌吉州）。
8. **站名统一带「站」后缀**；**线路名变体归一**（包兰线→包兰铁路 等）。
9. **v2.14 铁律**：① 区级城市键须并入地级市键；② 锚点站插入新线后须回写 `STATION_INFO[站].lines` 归属；③ 引擎阶段四（KSP 遮蔽兜底）代表站取法必须确定性（`min()`），否则测试抖动。
10. **合并后三项自检**：① LINE_ORDER↔lines 归属一致性（本批新增 MISS=0）；② 全部新站 BFS 到主干枢纽可达（防孤岛子网）；③ 自测连跑 ≥3 次确认无随机抖动。
11. **沙箱限制**：端口占用只能宿主机 `taskkill`；本地服务未启动先启动（`uvicorn web_app:app --host 0.0.0.0 --port 8080`，被占用按 +1 递增）。

---

## 附录 B：目录结构与关键文件

```
交接文件夹第三版（含一、二合并）/
├── src/
│   ├── railway_data.py          # 数据层（当前 v2.20）
│   ├── railway_data_v2.17.bak   # v2.17 基线（甘宁批备份）
│   ├── railway_data_v2.18.bak   # v2.18 基线
│   ├── railway_data_v2.19.bak   # v2.19 基线
│   ├── student_ticket_checker.py# 引擎 + 27 自测
│   ├── web_app.py               # Starlette Web（勿随意改）
│   ├── data/*.json              # 7 个镜像 JSON
│   ├── templates/*.html
│   └── static/style.css
├── merge_gs_nx_v218.py          # 任务七合并脚本（甘宁）
├── merge_qz_xz_v219.py          # 任务八合并脚本（青藏）✅ 已跑通
├── merge_xinjiang_v220.py       # 任务九合并脚本（新疆）✅ 已跑通
├── merge_gs_nx_v218_changelog.md# 任务七日志 ✅
├── merge_*_v2xx_changelog.md    # 历史批次日志（v2.9~v2.18 等）
└── 项目上下文压缩说明书.md       # 本文件
```

## 附录 C：合并脚本复用要点（数据源常见坑）

| 坑 | 表现 | 解法（已在技能中沉淀） |
|---|---|---|
| 标题行非第 0 行 | 混入「车站名称站」伪站 | 扫描定位「车站名称」标题行，从其下一行取数据 |
| 站名无「站」后缀 | 解析/接网失败 | `norm_sta` 统一补后缀再入库 |
| 省市前缀短写 | 「宁夏吴忠市…」漏解析 | `parse_city` 兼容 `宁夏` 短前缀 |
| 城市键格式随省不同 | 并入键与库内不一致 | 合并前先抽查该省既有键格式，跟随既有 |
| 线路名变体 | 包兰线/干武线/宝兰客专 | `norm_line` 归一 |
| 兵团/自治州键 | 第十四师被贪婪正则误吞 | `^新疆生产建设兵团[^师]*师` 而非 `第.师` |
| 孤岛子网 | 内部互连但到不了主干 | 补 `__SYN__主干线__锚点A__锚点B` 接网 |
| orphan 站 | 在 SI 但不在任何 LINE_ORDER | 补 SPLICE 到对应线序列 |
