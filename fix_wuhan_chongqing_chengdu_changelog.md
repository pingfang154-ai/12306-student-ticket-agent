# 修复变更日志：武汉 → 成都 经重庆 路径不可用（v2.7 → v2.8）

- 日期：2026-07-26
- 触发：用户临时插入任务，中止省份合并；要求「武汉至成都且经过重庆」能被正确识别为合理径路。
- 修复脚本：`fix_wuhan_chongqing_chengdu.py`
- 数据层备份：`src/railway_data_v2.7.bak`

---

## 一、问题现象

在 v2.7 数据下，学生票引擎对优惠区间「学校=武汉市 ↔ 家庭=成都市」做判定时：

1. **KSP 永远走达州绕行**：`find_multiple_paths_between_cities('武汉市','成都市')` 返回的首选路径是
   `汉口 → 襄阳 → 十堰 → 安康 → 达州 → 遂宁 → 成都`（12~17 跳），**完全不途经重庆**。
2. **「经重庆」无法被识别**：`resolve_location("重庆")` 仅返回 `{重庆站}`，而非重庆市全部车站集合；
   即便手动指定 `via=["重庆"]`，引擎也无法把「经重庆北」匹配为「途经重庆」。

这导致真实存在的「汉口—仙桃西—荆州—宜昌东—恩施—利川—丰都—重庆北—遂宁—成都东」沪汉蓉（宁蓉铁路）走廊
被引擎忽略，与真实客运常识（武汉→重庆→成都 主通道）相悖。

---

## 二、根因分析

### 根因 A：干线走廊被拆碎 + 均匀计权，Dijkstra 偏爱「边少」的达州绕行
宁蓉铁路（沪汉蓉快速客运通道）在库中被拆成 `汉宜铁路 + 宜万铁路 + 渝利铁路 + 成遂渝铁路` 等多段，
各段在 `LINE_ORDER` 中独立成线、且相邻两站之间计权相同（无里程权重）。
由于达州绕行（襄渝+达成）每段边数更少，Yen-KSP / Dijkstra 在「最短跳数」口径下自然选了它，
而真正的重庆走廊因被拆段、跳数分散，反而落败。

### 根因 B：`resolve_location` 城市键被同名车站遮蔽（优先级 bug）
原函数解析顺序是：
```
1) 精确站名命中 _ALL_STATIONS
2) q+'站' 站名回退           ← 命中库内「重庆市」站，只返回其所在区
3) CITY_TO_STATIONS 城市键    ← 新建的「重庆」城市键在这里，被步骤 2 抢先
4) CITY_ALIAS 别名
```
库内存在名为「重庆市」的车站，`resolve_location("重庆")` 在步骤 2 就命中 `+站` 回退，
返回该站所属区（渝中区）的车站集合，**遮蔽了步骤 3 的城市键「重庆」**，
导致「重庆」无法解析为全市车站集合。

---

## 三、修复方案

### 修复 A：补入「宁蓉铁路」干线走廊（网络层）
在 `LINE_ORDER` 中新增一条代表沪汉蓉主通道的干线走廊，使「经重庆」路线成为引擎可达的**最短合理径路**：

```python
LINE_ORDER["宁蓉铁路"] = ["汉口站","仙桃西站","荆州站","宜昌东站",
                          "恩施站","利川站","丰都站","重庆北站","遂宁站","成都东站"]
```
- 序列经联网核验（宁蓉铁路 / 沪汉蓉 全线主要大站：汉口→仙桃西→荆州→宜昌东→恩施→利川→石柱县→丰都→涪陵北→长寿北→重庆北→合川→潼南→遂宁→成都东），取武汉↔成都 区间主走廊 10 站。
- 该走廊仅 9 跳，短于旧「达州绕行」(12~17 跳)，KSP 自然将其列为首选。
- 新增 7 条边（均为既有相邻站间直连，增量加边、绝不删边）：
  `(仙桃西,汉口)`、`(仙桃西,荆州)`、`(利川,丰都)`、`(恩施,宜昌东)`、`(荆州,宜昌东)`、`(重庆北,丰都)`、`(重庆北,遂宁)`。

### 修复 B：统一「重庆」城市（数据层）
```python
CITY_TO_STATIONS["重庆"] = ['沙坪坝站','重庆北站','重庆站','重庆西站']
CITY_ALIAS["重庆市"] = "重庆"
```
将主要重庆站（重庆站/重庆北/重庆西/沙坪坝）从市辖区提升归集到城市键「重庆」，并加别名使 `home`/`via` 用「重庆市」亦可解析。

### 修复 C：修正 `resolve_location` 优先级（引擎层）
将城市键检查提前到 `+站` 站名回退之前：
```python
if q in _ALL_STATIONS:
    return set(CITY_TO_STATIONS.get(STATION_INFO[q]['city'], []))
if q in CITY_TO_STATIONS:                       # ← 城市键优先
    return set(CITY_TO_STATIONS[q])
if q in CITY_ALIAS:                             # ← 别名其次
    return set(CITY_TO_STATIONS[CITY_ALIAS[q]])
if (not q.endswith('站')) and (q+'站') in _ALL_STATIONS:   # ← 站名回退最后
    return set(CITY_TO_STATIONS.get(STATION_INFO[q+'站']['city'], []))
```
原则：**凡是「城市名解析」与「站名+站回退」可能冲突，一律城市优先**，避免同名车站遮蔽城市键。

---

## 四、铁律遵守

| 铁律 | 本次遵守情况 |
|------|--------------|
| 仅增量、只加不删 | ✅ 仅新增宁蓉走廊线 + 7 条边 + 1 城市键 + 1 别名，未删任何旧边/旧站 |
| 新线须写入 LINE_ORDER（Dijkstra 才可见） | ✅ `宁蓉铁路` 写入 `LINE_ORDER`，边由引擎重建 |
| 再生 railway_data.py 按内容标记截取 tail | ✅ 脚本仅 replace 7 个单行 dict，helper 尾部（含 `resolve_location`）原样保留 |
| 每次改动跑 27 条回归 | ✅ 27/27 通过，0 失败 |
| 枢纽解析须与上一版一致 | ✅ 13/192 未解析，与 v2.7 备份集合完全一致，非回归 |

---

## 五、验证结果

1. **回归**：`python -u src/student_ticket_checker.py --test` → **27/27 通过，0 失败**。
2. **城市解析**：
   - `resolve_location("重庆")` → `{重庆西站, 重庆站, 沙坪坝站, 重庆北站}`
   - `resolve_location("重庆市")` → 同全集（别名生效）
3. **KSP 首选路径（武汉市↔成都市）**：
   ```
   path1 (10站): 汉口站 → 仙桃西站 → 荆州站 → 宜昌东站 → 恩施站 → 利川站
                 → 丰都站 → 重庆北站 → 遂宁站 → 成都站
   含重庆北: True | 经重庆判定: True
   ```
   （path2/path3 同样经重庆北，仅局部站点不同）
4. **check_compliance 5 场景**：
   | 区间 | via | 结果 |
   |------|-----|------|
   | 汉口站 → 成都东站 | — | ✅ 符合 |
   | 汉口站 → 重庆北站 | — | ✅ 符合 |
   | 重庆北站 → 成都东站 | — | ✅ 符合 |
   | 荆州站 → 重庆北站 | — | ✅ 符合 |
   | 汉口站 → 成都东站 | 重庆 | ✅ 符合（path 含重庆） |
5. **枢纽解析**：192 枢纽中 179 可解析，13 个未解析（广州白云站、重庆东站、遵义南站、石家庄北站、哈尔滨东站、齐齐哈尔南站、武汉东站、徐州北站、扬州东站、呼和浩特站、大同东站、重庆沙坪坝站、北京东站）——与 v2.7 备份集合一致，**非回归**。
6. **连通性**：宁蓉 10 站全部连通北京/上海/广州。
7. **数据完整性**：6 个单行 dict（META/LINE_ORDER/STATION_INFO/CITY_TO_STATIONS/PROVINCE_TO_STATIONS/CITY_ALIAS/GRAPH）各仅 1 次，helper 尾部完整；4 个 JSON（lines_order/graph_adjacency/station_info/city_to_stations）已同步。

---

## 六、Web 查验（v2.8 服务）

- 启动：`uvicorn src.web_app:app --host 127.0.0.1 --port 8082`（日志 `/tmp/uvicorn_8082.log`）
- 8080（v2.6 旧）、8081（v2.7 旧）仍被宿主机旧进程占用，沙箱无法 kill，v2.8 临时起 8082。
- 实测：
  - `GET /check?school=武汉市&home=成都市&dep=汉口站&arr=成都东站` → 渲染「符合」，路径含重庆北站
  - `GET /check?school=武汉市&home=成都市&dep=汉口站&arr=成都东站&waypoints=重庆` → 渲染「途经重庆」
  - `GET /cases` → 200

### 查验链接
主页面：http://localhost:8082
案例库：http://localhost:8082/cases
功能直达（武汉→成都经重庆）：http://localhost:8082/check?school=武汉市&home=成都市&dep=汉口站&arr=成都东站&waypoints=重庆

> 注：需先在本地运行 `uvicorn src.web_app:app --port 8082` 启动服务（8080/8081 为旧版本，宿主机 taskkill 释放后可统一回 8080）。

---

## 七、规模变化（v2.7 → v2.8）

| 指标 | v2.7 | v2.8 |
|------|------|------|
| 线路数 | 395 | 396（+1 宁蓉干线走廊） |
| 车站数 | 1723 | 1723（无新增站，仅重组归属） |
| 图节点 | 1723 | 1723 |
| 可解析枢纽 | 179/192 | 179/192（一致） |
